import { RetroGrid } from "@/components/elements/RetroGrid"

export const HomePage = () => {
    return (
        <div className="flex h-screen items-center justify-center">
            <RetroGrid />
        </div>
    )
}